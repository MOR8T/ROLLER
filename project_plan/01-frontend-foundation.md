# Этап 01 — Фундамент фронтенда

**Фаза:** 1 (Frontend) · **Зависит от:** — · **Статус:** в основном выполнен

## Цель
Основа Next.js-приложения: дизайн-токены, структура папок, базовый UI-kit,
моковые данные. После этого этапа верстка любой страницы — сборка из готовых
блоков и токенов.

## Предусловия
- В `frontend/` есть Next.js 16 + Tailwind v4.
- Прочитан `frontend/AGENTS.md` и `frontend/DESIGN.md`.

## Выполнено
- [x] Prettier + `prettier-plugin-tailwindcss`, скрипты `format` / `format:check`.
- [x] Переименование проекта в `roller-tj`, очистка стартового шаблона.
- [x] Структура папок: `app/`, `components/{ui,layout,sections}/`, `lib/`, `data/`, `types/`.
- [x] Дизайн-токены в `globals.css` (`@theme`): цвета бренда + нейтральная шкала.
- [x] Шрифты `Chakra Petch` + `Montserrat` через `next/font/google`.
- [x] `lib/site-config.ts` — контакты, соцсети, WhatsApp, адрес, график, карта, слоган.
- [x] `lucide-react`, `framer-motion`, `swiper`.
- [x] Базовый layout: `Header` + `Footer`.
- [x] UI-компоненты: `Button`, `Container`, `Section`, `Card`, `Badge`, `Input`,
      `Textarea`, `Select`, `MediaFrame`, `Reveal`, `BrandLogo`.

## Задачи к доработке

### Токены под `DESIGN.md`
- [ ] Шкала радиусов: **8–12px** для карточек и медиа, **8px** для кнопок и полей.
      Убрать `rounded-full` у кнопок и `rounded-3xl` у карточек.
- [ ] Токен контейнера и единая шкала вертикальных отступов секции.
- [ ] Семантические токены фона: `surface` (белый), `surface-muted` (`neutral-50`),
      `surface-inverse` (`#1D1D1B` — только «Профессионалам» и футер).

### Шрифты — подмножества
- [x] Проверено сборкой: `subsets` в `next/font` управляет **предзагрузкой**,
      а не составом `@font-face`. Блоки эмитятся для всех подмножеств шрифта,
      браузер догружает нужный файл по `unicode-range`. Таджикские
      **ғ қ ҳ ҷ ӣ ӯ** и турецкие **ğ ş İ** отрисовываются Montserrat'ом при
      любом списке `subsets` — в системный шрифт они не выпадают.
- [x] Оставлено `Montserrat: ["latin", "cyrillic"]`, `Chakra Petch: ["latin"]` —
      перечисление всех подмножеств удваивает предзагрузку с 6 до 12 файлов
      на каждой странице, включая русские.
- [x] Зафиксировано в коде: `--font-heading` применяется, но кириллица всегда
      рендерится Montserrat'ом — это ожидаемое поведение, не баг.
- [ ] Предзагрузка по локали — перенесено в **этап 03**, требует `[locale]`.

### Разное
- [ ] `next.config.ts`: `images.remotePatterns` — при подключении внешних фото
      (понадобится на этапе 13, когда изображения поедут из API).
- [ ] Проверить `Header` (546 строк) — вынести мега-меню и мобильное меню
      в отдельные компоненты.

## Результат / артефакты
- Проект с дизайн-системой, соответствующей `DESIGN.md`.
- UI-kit, `siteConfig`, типы, моки.

## Критерии готовности
- `npm run dev` / `build` / `lint` / `format:check` — чисто.
- Токены применяются через Tailwind-классы, «магических» значений нет.
- Радиусы и подмножества шрифтов соответствуют `DESIGN.md`.

## Заметки
- i18n-библиотека зафиксирована: **`next-intl`**. Сам каркас — этап 03.
- Не тянуть тяжёлые UI-библиотеки.
