/**
 * The mega-menu panel's DOM id. The trigger points `aria-controls` at it, so
 * the two files have to agree on one string.
 *
 * There is deliberately no `PRODUCTS_HREF` any more: «Продукция» opens the
 * panel and nothing else — see `navLinks` in `lib/site-config.ts`.
 */
export const PRODUCTS_MENU_ID = "products-mega-menu";
